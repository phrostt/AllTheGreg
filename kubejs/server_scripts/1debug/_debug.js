ServerEvents.recipes(event => {
    /*event.forEachRecipe({ type: 'botania:mana_infusion' }, recipe => {
        console.error(recipe.id.toString());
        console.error(recipe.json.toString());
        console.error('---');
    });
    console.error('scan finished');*/

});