ServerEvents.recipes(event => {
    /*event.forEachRecipe({ type: 'occultism:ritual' }, recipe => {        
        console.error('RJSON:' + recipe.json.toString());        
    });
    console.error('scan finished');*/
});